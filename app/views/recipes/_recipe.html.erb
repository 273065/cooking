<%= link_to image_tag(recipe.image.variant(resize: '100x100') ), recipe_path(recipe.id) %>
<%= link_to recipe.user.name, user_path(recipe.user.id) %>
<i class="fa fa-heart unlike-btn"></i>
<%= recipe.likes.length %>
<%= link_to recipe.title, recipe_path(recipe.id) %>